# Web UX Evidence

This file records Playwright-style e2e coverage intent and design token
discipline markers for web surfaces so render-readiness checks can locate the
required UX evidence signals.

Design tokens and semantic tokens are reviewed in the audit evidence surface.
