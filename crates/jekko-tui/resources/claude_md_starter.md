# Project Overview

Brief description of what this project does and why it exists.

## Build & Test

Common commands the agent should know about for this project. Add the build,
test, lint, and format commands here so the agent can run them without asking.

- Build:  `<build command>`
- Test:   `<test command>`
- Lint:   `<lint command>`
- Format: `<format command>`

## Conventions

House rules the agent should follow when editing this codebase:

- Code style or formatter rules
- Naming conventions
- File/module layout expectations
- Branch / commit message format

## Notes

Anything else the agent should know: gotchas, in-flight refactors, third-party
docs links, points of contact, etc.
