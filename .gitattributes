# Deterministic line endings for ZYAL sources and any YAML emitted by zyalc.
# Without this the cross-platform `zyalc compile --check` lane will trip on
# Windows checkouts.
*.zyal text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
